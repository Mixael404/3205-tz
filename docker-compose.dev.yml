services:
  backend:
    build:
      target: dev
    restart: unless-stopped
    volumes:
      - ./backend:/app
      - /app/node_modules

  frontend:
    build:
      target: dev
    restart: unless-stopped
    volumes:
      - ./frontend:/app
      - /app/node_modules